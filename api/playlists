
/* 
export const playlists = async () => {
    let url = "http://localhost:3000/playlist";
    try {
      const response = await fetch(url);
      if (!response.ok) {
        const errorData = await response.json();
        throw errorData.error;
      }
      return await response.json();
    } catch (error) {
      throw error;
    }
  }; */